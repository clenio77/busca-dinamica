{
  "version": 2,
  "buildCommand": "cd busca-dinamica-react && npm install && npm run build",
  "outputDirectory": "busca-dinamica-react/build",
  "installCommand": "cd busca-dinamica-react && npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/cep/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
