######################################################
# Arquivo de Exemplo de Variáveis de Ambiente (.env) #
######################################################

# Porta que o servidor irá escutar
PORT=3000

# Caminho do banco de dados SQLite (ambiente local)
DB_PATH=./database/ceps.db

# URL de conexão PostgreSQL (produção ou Render)
# Formato: postgres://usuario:senha@host:5432/nome_db
DATABASE_URL=

# Token de autenticação para rotas /api/admin
ADMIN_TOKEN=troque_este_token

# Arquivo de log principal
LOG_FILE=./logs/app.log

# Cron para execução semanal do agente de atualização
# Padrão: Segunda-feira às 02:00 (horário de São Paulo)
UPDATE_SCHEDULE=0 2 * * 1

# Configurações do scraper
SCRAPER_DELAY_MS=1000
MAX_REQUESTS_PER_MINUTE=60

# Base URLs das APIs externas (pode customizar se necessário)
VIACEP_BASE_URL=https://viacep.com.br/ws
BRASILAPI_BASE_URL=https://brasilapi.com.br/api
